/*
 * Controlleur.java                      				            25 mai 2023
 * IUT de Rodez, pas de copyright ni de "copyleft"
 */
package application.controlleurs;

import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.application.Application;
import javafx.stage.*;
import javafx.scene.Scene;
import javafx.scene.Parent;
import javafx.scene.text.Text;

/**
 * Contrôle via FXML les interactions avec la vue : les pages FXML
 * 
 * @author Tom Douaud
 * @author Loïc Faugières
 * @author Simon Guiraud
 */
public class ControlleurParametres {

	@FXML
	private Text menuprincipal_infospartie;
	
	@FXML
	private void clickParametres() {		
		menuprincipal_infospartie.setText("test");
		
		// Création d'un chargeur de code FXML
	    FXMLLoader chargeurFXML = new FXMLLoader();
	    
	    chargeurFXML.setLocation(this.getClass().getResource("Parametres.fxml"));
	    
	    Parent racine = chargeurFXML.load();
	    
		Scene scene = new Scene(racine);
		scene.getStylesheets().add(getClass().getResource("application.css").toExternalForm());
		
		primaryStage.setTitle("Othello - Paramètres");
		primaryStage.setScene(scene);
		primaryStage.show();
	}
	
	@FXML
	private void generer() {
		
	}

}